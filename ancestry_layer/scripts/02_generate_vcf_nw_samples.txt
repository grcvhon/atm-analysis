# Clean up VCF files via "Filter by individual" (i.e., using a keep list)
# to only keep nw samples

# use vcftools
# check directory

vcftools --vcf ./vcf-files/updated/ALA-stringent.vcf --keep ./sample-lists/ALA-nw.txt --recode --stdout > ./ALA-stringent.nw.keep.vcf

vcftools --vcf ./vcf-files/updated/HMA-stringent.vcf --keep ./sample-lists/HMA-nw.txt --recode --stdout > ./HMA-stringent.nw.keep.vcf

vcftools --vcf ./vcf-files/updated/HST-stringent.vcf --keep ./sample-lists/HST-nw.txt --recode --stdout > ./HST-stringent.nw.keep.vcf